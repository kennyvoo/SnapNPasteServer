
from collections import defaultdict

# Using dictionary to simulate database first :)

db = defaultdict(lambda: defaultdict(dict))

# from collections import defaultdict

# db=defaultdict(lambda: defaultdict(dict))

# db["kenny"]["car"]=1
# db["kenny"]["dan"]=2
# for item,w in db["kenny"].items():
#     print(item)
